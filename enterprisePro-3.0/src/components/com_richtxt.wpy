

<template>
  <view class="f32 pw20" style='word-break: break-all'>
    <import src="../../lib/wxParse/wxParse.wxml" />
    <repeat wx:for="{{currentCom.content_fmt.nodes||currentCom.nodes}}" wx:key="{{index}}">
      <template is="wxParse0" data="{{item}}" />
    </repeat>
  </view>
</template>

<script>
  import wepy from "wepy";
  export default class com_richtxt extends wepy.component {
    components = {};
    props = {};
    methods = {
    
    };
  }
</script>