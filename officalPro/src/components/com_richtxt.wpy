

<template>
  <view  style='padding:0 30rpx;word-break: break-all'>
    <import src="../../lib/wxParse/wxParse.wxml" />
    <repeat wx:for="{{currentCom.content.nodes}}" wx:key="{{index}}">
      <template is="wxParse0" data="{{item}}" />
    </repeat>
  </view>
</template>

<script>
  import wepy from "wepy";
  export default class com_richtxt extends wepy.component {
    components = {};
    props = {};
    methods = {
     
    };
  }
</script>